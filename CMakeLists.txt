SET ( CMAKE_VERBOSE_MAKEFILE true )
SET ( RUNTIME_OUTPUT_DIRECTORY output/bin )
# ADD_DEFINITIONS( -DICACHE_FLASH -DMY_DEBUG=1 )
INCLUDE_DIRECTORIES ( packet/inc )
SET ( PACKET_PATH packet/src)
SET ( TEST_PATH test)
ADD_EXECUTABLE( app.out ${TEST_PATH}/main.c ${PACKET_PATH}/packet.c )